pnpm 패키지 매니저 사용

디스크 용량 적고 설치 속도 빠름

FSD 구조 사용

pages(=>views로 이름 변경, Next의 page라우팅에 예약된 라우팅폴더 명칭이라 사용불가),widgets,features,entitles,shared 레이어사용 (일단 다 해보고 나중에 불필요하면 없애가기)

Tailwind CSS 사용 

SSR과도 궁합이 잘 맞음

별도 스타일 파일 필요없음

TanStack 라이브러리 사용

라우팅은 Next가 알아서해주므로 별도 라이브러리 필요없음
